namespace PharmacyCore.Stocks

// TODO: Think about extracting the multi-level packaging domain model from the Drug module,
// so that it can be reused by Products (para, OTC), and also used maybe for tracking stock?
// Maybe it simplifies stock tracking. Also for normal products, we can use a single-level
// packaging using the same packaging domain model.

module ProductStock =
  ()
